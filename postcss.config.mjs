/**
 * Tailwind đã được gỡ bỏ khỏi dự án — không còn plugin PostCSS nào.
 * CSS được viết thuần (BEM) trong globals.css, ui.css, home.css, jobs.css.
 */
const config = {
  plugins: [],
};

export default config;
